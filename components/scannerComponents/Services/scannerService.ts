// components/scannerComponents/Services/scannerService.ts
export function saveScannedContact(data: string): Promise<void> {
  // Placeholder until a real contacts backend exists.
  return Promise.resolve();
}
